<div class="show-wrapper">
  <%= render 'shared/searchbar' %>
  <div class="word-show rectangle">
    <h1><%= @word.word %></h1>
    <i class="fas fa-volume-up orange"></i>
  </div>
  <% @word.kor_nuances.each do |nuance| %>
    <div class="blue-rectangle nuance-box">
      <div class="nuance-header">
        <span style="display: flex" class="bold">
          <h3 class="hanjas blue">
            [
            <% nuance.hanjas.each do |hanja| %>
              <%= hanja.hanja %>
            <% end %>
            ]
          </h3>
          <h3 class="blue"><%= @word.word %></h3>
          <h3 class="light" style="padding-left: 20px;"><%= "(#{nuance.nature_francais} | #{nuance.nature_cible})" %></h3>
        </span>
        <% if nuance.image_url %>
          <i class="fas fa-image fa-2x nuance-image">
          </i>
        <% end %>
      </div>
      <h3 class="mediumsized"> => <%= nuance.traduction %></h3>
      <hr>
      <h4 class="light"><%= nuance.commentaire %>.</h4>
      <hr>
      <% nuance.kor_exemples.each do |exemple| %>
        <div class="exemple">
          <h5 class="mediumsized blue">
            <%= exemple.exemple_cible %>
          </h5>
          <h5 class="blue mediumsized">
             <%= exemple.exemple_traduction %>
          </h5>
        </div>
      <% end %>
      <h5 class="blue light seemore">Voir plus d'exemples</h5>
      <hr>
      <h5 class="light blue"><%= nuance.remarque %></h5>
      <hr class="hrthick">
      <div>
        <h4 class="mediumsized">Ces synonymes pourraient vous intéresser:</h4>
        <% nuance.kor_synonymes.each do |synonyme| %>
          <h5 class="blue light syno">
            <%= synonyme.synonyme %>
            <% if synonyme == nuance.kor_synonymes[-1] %>
              .
            <% else %>
              ,
            <% end %>
          </h5>
        <% end %>
      </div>
      <hr>
      <div>
        <h4 class="mediumsized">Ces antonymes pourraient vous intéresser:</h4>
        <% nuance.kor_antonymes.each do |antonyme| %>
          <h5 class="light blue anto">
            <%= antonyme.antonyme %>
            <% if antonyme == nuance.kor_antonymes[-1] %>
              .
            <% else %>
              ,
            <% end %>
          </h5>
        <% end %>
      </div>
    </div>
  <% end %>
  <div class="suggestions">
     <h3>Suggerer un mot | faire part d'une erreur</h3>
  </div>
</div>
